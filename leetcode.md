# leetcode 笔记
leetcode144：
重点是对于左右子树如何处理，我需要把左值挂靠在当前的右树，原来的右树则需要保存下来，又因为右树最后需要挂靠在左树的最右端，那么就选择直接找到这个值，然后直接将右树接在这个节点右端

leetcode173:
设计一个类来实现相关的函数的操作。中序遍历的迭代写法：先从root将所有的left存入栈，当存入的节点没有左孩子时，将当前节点弹出，如果其有右孩子，那么将其右孩子入栈，一直到栈为空且root为空。那么对于这道题，需要我在维护这个栈的同时，只有当调用nxt时才会执行出栈以及出栈后的操作。